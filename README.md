Gibbon is a lightweight scala library that makes it easy to create production ready, backpressure aware, stateful, event-time-correct streaming pipelines that can run locally for dev, or across a cluster.

Still a WIP